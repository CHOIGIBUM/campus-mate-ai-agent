# Harness Workspace

Runtime audit artifacts are written under `runs/<run-id>/` according to `workflow.md`.

This directory is gitignored except for this README and `.gitkeep`. Never place credentials or raw private user data here.
